{
  "name": "cerroni-solutions",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh": "tsc && vite build --emptyOutDir false",
    "preview": "vite preview",
    "deploy": "npm run build:gh && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.162.0"
  },
  "devDependencies": {
    "@types/three": "^0.162.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
